#include<stdio.h>
int main()
{
    int arr[] = {24,5,8,27};
    int n = sizeof(arr) / sizeof(arr[0]);
    int index = 1;
    if(index>=0 && index<n)
    {
        printf("%d",arr[index]);
        
    }
}
