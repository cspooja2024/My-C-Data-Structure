include<stdio.h>
#define CAP 11
void pooja(int a[], int n);
void pooja(int a[CAP], int n){
    for(int i=0; i<n && i<CAP; i++){
        a[i]=i;
        printf("%d ",a[i]);
    }
}
int main() {
    int a[CAP], n=160;
    pooja(a,n);
}
